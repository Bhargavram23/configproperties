msg: Janaki Pati User
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  boot:
    admin:
      client:
        url: http://localhost:1111/
  zipkin:
    base-url: http://localhost:9411/
    enabled: true
    service.name: USER_SERVICE
    sender.type: web
  sleuth:
    enabled: true
    sampler:
      probability: 1.0		
management:
  endpoints:
    web:
      exposure:
        include: '*'
